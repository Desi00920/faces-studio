// Stubbed — tRPC googleReviews endpoint removed (Vercel 12-function limit).
// Reviews will be re-added later, fetched directly from Google Places API or static data.
export default function GoogleReviews() {
  return null;
}
